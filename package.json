{
  "name": "check-versions-in-packagejson",
  "version": "1.2.2",
  "description": "Check versions of dependencies in your package.json ",
  "keywords": [
    "check",
    "version",
    "check-version",
    "package",
    "package.json"
  ],
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "check-versions": "./bin/check-versions",
    "checkversions": "./bin/check-versions"
  },
  "author": "bowencool",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.4.2",
    "check-versions-in-packagejson": "^1.2.2",
    "console-table-printer": "^2.10.0",
    "semver": "^6.3.0"
  }
}
