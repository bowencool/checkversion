{
  "name": "check-versions-of-dep",
  "version": "1.1.0",
  "description": "Check versions of dependencies in your package.json ",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "check-versions-of-dep": "./bin/checkversion",
    "check-versions": "./bin/checkversion",
    "cv": "./bin/checkversion"
  },
  "author": "bowencool",
  "license": "MIT",
  "devDependencies": {
    "chalk": "^2.4.2",
    "semver": "^6.3.0",
    "yargs": "^14.0.0"
  }
}
